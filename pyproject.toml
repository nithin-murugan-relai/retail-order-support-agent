[project]
name = "relai-retail-support-agent"
version = "0.1.0"
description = "Terminal retail order-support agent for RELAI learning loops."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "openai-agents[litellm]>=0.3.0",
  "python-dotenv>=1.0.1",
]

[project.scripts]
retail-support = "retail_support.main:run"

[dependency-groups]
dev = [
  "pytest>=8.3.0",
  "pytest-asyncio>=0.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/retail_support"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
