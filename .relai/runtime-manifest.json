{
  "schema_version": "relai.runtime_manifest.v1",
  "env": [
    {
      "name": "OPENAI_API_KEY",
      "required": false,
      "sensitive": true,
      "reason": "One of OPENAI_API_KEY or ANTHROPIC_API_KEY must be set so retail_support.models.select_model() can choose a live provider. OpenAI takes precedence when both are present."
    },
    {
      "name": "ANTHROPIC_API_KEY",
      "required": false,
      "sensitive": true,
      "reason": "One of OPENAI_API_KEY or ANTHROPIC_API_KEY must be set so retail_support.models.select_model() can choose a live provider. OpenAI takes precedence when both are present."
    }
  ],
  "files": []
}
